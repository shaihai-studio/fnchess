# 服务端权威化立项文档（#4 整改方案）

> 立项时间：2026-08-15
> 来源：大重构计划 #4（矛盾级，🔴）——服务端不参与对局却当权威裁判，是刷分漏洞的总根源。
> 本文档仅产出**方案与分期实施计划**。本轮（批次 13）**不动任何代码**，待用户拍板后单独排期实施。
> 关联发现：M10 期间同时暴露「竞速结算无限速（#2，P0）」「rtN 上限 1-30 运行时不一致」「verifySig 硬编码」等运行期问题，本方案聚焦 #4 架构级根因，运行期问题单列整改。

---

## 1. 现状（为什么是刷分总根源）

当前对局（闯关/竞速/P2P）全部在 **P2P 或本地** 跑：

- 闯关/竞速的**进度、名次、难度、耐力**全部在客户端手里；
- 服务端 `server/index.js` 的 `handleRaceScore` / `handleSubmitScore` 只是**套公式**——"你报什么分数/用时，我就信什么"；
- 服务端既**不参战**，也**不持有对局状态**，却作为榜单/ELO 的"权威裁判"落库。

因此理论上客户端可伪造任意分数、段位、竞速用时上报，服务端无能力鉴别真伪。这是所有刷分类漏洞的**架构级根因**，#2（竞速无限速）只是其表层表现之一。

---

## 2. 风险分级

| 等级 | 项 | 说明 |
|---|---|---|
| P0 | 任意刷分（分数/段位/榜） | 客户端自报即落库，无校验 |
| P0 | 竞速结算无限速（#2） | 竞速用时可由客户端自报，可伪造极短用时登顶 rtN 榜 |
| P1 | 匿名冒名 | 昵称仅长度截断，无身份绑定，可反复用小号刷 |
| P2 | 运行期不一致 | rtN 上限、verifySig 硬编码等（M10 已发现，另立整改） |

---

## 3. 整改方案（两阶段）

### 阶段一：服务器中继权威计时（先堵竞速刷分）

**目标**：竞速 Time Attack 的"用时"改由**服务端权威计时**，客户端无法自报。

- 竞速开局时，服务端生成 `raceSessionId` 并记录 `startTs`；
- 客户端完成关卡后上报 `raceSessionId`，服务端用 `now - startTs` 计算权威用时，写入 `rtN` 榜；
- 客户端上报的本地用时仅作兜底/展示，不参与排名计算；
- 配合 #2：服务端对单关/整局用时设下限阈值，低于阈值直接判无效（防极短伪造）；
- P2P 对战进度仍走 P2P，但 ELO 结算保持「房主上报 + roomKey 去重」对称公式（现有机制已对称，无需重构）。

**影响面**：仅竞速上报协议（`handleRaceScore`）。解锁/段位/闯关榜暂不强制服务端计时（闯关为单人进度，刷分危害较低，可后置）。

### 阶段二：账号体系（彻底消除匿名刷分）

**目标**：用账号/会话绑定身份，刷分需付出可追溯成本。

- 引入轻量 session/token（登录或设备绑定），上报必须带有效 token；
- 榜单按 `accountId` 聚合，小号刷分可被识别与合并；
- token 签发走服务端，杜绝客户端自签（呼应 M10「verifySig 硬编码」问题，改为服务端签发的可校验凭证）；
- 与阶段一可并存：竞速榜 = 服务端计时 + 账号绑定双重保险。

**取舍**：账号体系会引入隐私/合规与服务器存储成本，需用户确认是否纳入（纯前端游戏可先只做阶段一 + 设备指纹软绑定）。

---

## 4. 分期实施计划（草案，待拍板）

| 期 | 内容 | 依赖 | 风险 |
|---|---|---|---|
| 期 1（P0） | 阶段一：竞速服务端权威计时 + 用时下限阈值 | 无 | 低（仅竞速协议） |
| 期 2（P0） | #2 竞速结算限速正式落库校验 | 期 1 | 低 |
| 期 3（P1） | 设备指纹/软账号绑定，榜单按 identity 聚合 | 期 1 | 中 |
| 期 4（P2） | 运行期不一致修复（rtN 1-30、verifySig 硬编码） | 无 | 低 |
| 期 5（可选） | 阶段二：完整账号体系（登录/token） | 期 3 | 高（合规/存储） |

---

## 5. 本轮交付物

- ✅ 本文档（方案 + 分期计划）；
- ✅ `大重构计划.md` #4 状态回填为「⚠️ 已立项（2026-08-15 批次13 产出方案+分期计划，本轮不动代码）」；
- ❌ 暂不改 `server/index.js` 或任何前端代码——待用户对阶段一/阶段二拍板后单独排期。

---

## 6. 决策待用户确认（开放问题）

1. 是否先只做**阶段一（竞速权威计时）**即止，账号体系（阶段二）暂缓？
2. 闯关榜是否纳入服务端计时，还是保持本地（单人、危害低）？
3. 账号体系采用「完整登录」还是「设备指纹软绑定」？
