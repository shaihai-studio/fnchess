# 换服务器须知

如果你需要更换联机对战的信令服务器，请向开发者提供以下信息：

---

## 方案A：部署你自己的 PeerJS 信令服务器

需要提供：

1. **服务器信息**
   - 服务器 IP 地址或域名：`_________________________`
   - 服务器操作系统：`Linux / Windows / macOS`
   - Node.js 是否已安装？版本号：`____________________`
   - 是否有公网 IP？□ 是 □ 否（需额外内网穿透）

2. **端口 & 防火墙**
   - 希望使用的端口号（默认 9000）：`_________________`
   - 该端口是否已在防火墙/安全组放行？□ 是 □ 否

3. **SSL/HTTPS**（如果通过 HTTPS 访问前端，则必须开启）
   - 是否需要 SSL？□ 是 □ 否
   - 如果选是，SSL 证书是否已准备好？□ 是 □ 否（需提供证书路径或由开发者协助配置 Nginx 反代）

4. **启动方式**
   - 上传 `server/` 目录，执行 `npm install && npm start`
   - 或用 `npx peerjs-server --port 9000` 快速启动

---

## 方案B：使用其他公共 PeerJS 服务器

需要提供：

1. **服务器地址**
   - host（域名）：`_________________________`
   - port（端口）：`_________________________`
   - secure（是否 HTTPS）：□ 是 □ 否
   - path（路径，通常为 `/`）：`_________________`

2. **示例：当前使用的 `0.peerjs.com` 公共服务器**
   ```
   host: '0.peerjs.com'
   port: 443
   secure: true
   path: '/'
   ```

---

## 方案C：使用自定义信令服务器（非 PeerJS）

需要提供：

1. 信令协议说明或文档链接
2. 服务器地址、端口、认证方式
3. 是否有可用的 SDK/库（npm 包名或 CDN 地址）

---

## 修改位置

更换服务器后，需要修改以下文件中的配置：

| 文件 | 位置 | 说明 |
|------|------|------|
| `files/js/P2PController.js` | `P2PController.signaling` | 主配置入口，也可以通过 `window.P2P_SIGNALING` 在 HTML 中覆盖 |
| `server/index.js` | `PORT` / `HOST` | 自托管时修改监听端口 |

---

## 补充说明

- 当前默认使用 `0.peerjs.com` 公共服务器（免费、无需自托管）
- 信令服务器只负责交换连接元数据，游戏数据（函数表达式、棋盘状态）通过 P2P 直连传输
- 如果双方处于对称型 NAT 后面无法直连，还需额外部署 TURN 中继服务器
