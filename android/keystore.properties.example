# 复制为 android/keystore.properties 并填入你的发布签名信息（该文件已被 .gitignore 忽略，切勿入库！）
# 生成密钥（仅首次，在仓库根目录执行）：
#   powershell -ExecutionPolicy Bypass -File scripts\gen-android-keystore.ps1
# storeFile 相对于 android/app/ 解析；下面路径 = android/fnchess-release.keystore
storeFile=../fnchess-release.keystore
storePassword=你的密钥库口令
keyAlias=fnchess
keyPassword=你的密钥口令（与上面相同即可）
