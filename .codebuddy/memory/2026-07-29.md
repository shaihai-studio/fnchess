# 2026-07-29

## 完成高优先级 TODO

### 1. 抽取统一 prepareInputPhase()，消除两套表达式清理机制分叉

**问题**：之前存在两套并行的清理机制：
- 模型清理（roundState 字段重置）在竞速失败路径中内联进行
- UI 清理（clearExpression()）通过 forceClearExpression 事件分散在不同 handler 中
- campaignFail/raceLevelResult 中直接调 clearExpression() + setPhase()，绕过了集中控制

**方案**：
- GameController 新增 `prepareInputPhase()` 方法：统一做模型清理 + emit prepareInputPhase 事件 + setPhase(INPUT_FUNCTION)
- UIController 统一监听 prepareInputPhase 事件，只做 UI 侧 clearExpression()
- 替换了 4 处调用点：竞速失败重试、闯关失败、竞速关卡结果失败、超时处理器

**修改文件**：
- files/js/GameController.js: 新增 prepareInputPhase()，替换 3 处旧调用
- files/js/UIController.js: 替换 forceClearExpression 处理器，替换 2 处旧调用

forceClearExpression 事件已完全从代码库中移除。

### 2. 以当前代码为准重写游戏规则.txt

完全基于代码实际行为重写，覆盖：
- 核心概念：棋盘、函数表达式、元素锁定、回合流程（含前4回合热身期特殊规则）
- 表达式语法：所有支持的运算符、函数、常量（包括 i/e/π）
- 命中判定机制（视觉采样法、适应容差）
- 得分体系（函数类型评分/简洁度评分）
- 6 种游戏模式详细配置表
- 规则细节（选格、元素锁定、限时、usedCells 等）
- 技术说明（供开发者参考）
