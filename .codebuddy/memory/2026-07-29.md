# 2026-07-29 工作记录

## 函数棋项目

- 在根目录创建 `Goal.md`：项目交接手册（现状/架构/踩坑/决策/待办/TODO/对接 checklist）。
- 此前已完成的 10 项 bug 修复（见 `问题清单与待解决项.md`）确认已落地：
  - ① 改 `游戏规则.txt` 对齐代码（回合1起可设禁区）
  - ②⑧ `GridSystem.evaluateExpression` 改用 `FunctionParser` AST 求值
  - ③ `resetGame` 补 `elementLockCounts = new Map()`
  - ④ 核查 `this.timer` 守卫 bug 已不存在，无需改动
  - ⑤ 容差维持现状（宽松像素，不改为规则单位）
  - ⑥ 已删 `RoundStateMachine.js` 死文件 + 移除 index.html 引用
  - ⑦ 误报排除（二分断点算法）
  - ⑨ `GridSystem` 新增 `pxPerUnit`/`cellSizePx` 单一换算源
  - ⑩ 竞速失败分支内联重置 `hitTargets/hitTarget/hitForbidden`
- 关键坑（已写入 Goal.md 第4节）：游戏规则.txt 游戏内不加载；容差单位陷阱；FunctionParser 脚本加载顺序晚于 GridSystem 但惰性实例化 OK；两套表达式清理机制并存。
- Git 状态：上述改动均未提交（working tree 改动），建议评审后 commit。
- 项目为纯前端静态站点，无构建/无单测；联机需 `server/` PeerJS 信令服务器。
